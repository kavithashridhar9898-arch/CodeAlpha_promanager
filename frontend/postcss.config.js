// Tailwind v4 uses @tailwindcss/vite for Vite projects.
// For PostCSS-based setups (non-Vite), use @tailwindcss/postcss instead.
// Since we're using Vite, the Tailwind Vite plugin handles CSS processing.
export default {};
